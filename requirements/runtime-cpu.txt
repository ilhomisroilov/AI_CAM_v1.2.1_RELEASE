# Explicit CPU wheels are the portable default and never require NVIDIA.
# The local production baseline was validated with this engine pair.
--extra-index-url https://download.pytorch.org/whl/cpu

torch==2.4.1+cpu; platform_system == "Windows" or platform_system == "Linux"
torchvision==0.19.1+cpu; platform_system == "Windows" or platform_system == "Linux"
torch==2.4.1; platform_system == "Darwin"
torchvision==0.19.1; platform_system == "Darwin"
paddlepaddle==2.6.2
